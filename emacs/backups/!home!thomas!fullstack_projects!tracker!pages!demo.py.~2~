import streamlit as st
import pandas as pd

from pages.utils import init_connection, get_git_hash

conn = init_connection()

def app():
    st.header("Dummy page")

    most_recent_workout = pd.read_sql("""
    select workout_date
    from workouts
    order by workout_id desc
    limit 1
    """, conn)

